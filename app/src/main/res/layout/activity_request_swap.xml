<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Request Shift Swap"
        android:textSize="24sp"
        android:layout_marginBottom="16dp" />

    <Spinner
        android:id="@+id/shiftFromSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Select Shift to Swap"
        android:layout_marginBottom="8dp" />

    <Spinner
        android:id="@+id/shiftToSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Select Desired Shift"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/submitSwapButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Submit Swap Request" />
</LinearLayout>